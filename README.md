# kaup39 — демо-версия сайта «Кауп»

Приватный репозиторий. Next.js 16 + TypeScript + SCSS-модули, один язык — русский,
темная тема основная, светлая вторая.

Решения по студии — в `../PLAN.md`, порядок работ и критерии приемки — в `../ROADMAP.md`
(фаза 6), артборд направления — `../design/kaup/Kaup.dc.html`.

**Чужой контент в git не попадает.** Тексты и фотографии владельцев живут в медиатеке CMS
на закрытом стенде и локально в `../research/kaup39/`. Репозиторий открывается из приватного
в публичный только после письменного разрешения владельцев.

## Запуск

```bash
corepack enable
yarn install
yarn dev            # http://localhost:3000
```

## Команды

| Команда                                          | Что делает                                                             |
| ------------------------------------------------ | ---------------------------------------------------------------------- |
| `yarn dev`                                       | дев-сервер                                                             |
| `yarn build`                                     | production-сборка                                                      |
| `yarn typecheck`                                 | `tsc --noEmit`                                                         |
| `yarn lint`                                      | ESLint (flat config, `eslint-config-next`)                             |
| `yarn format` / `yarn format:check`              | Prettier                                                               |
| `yarn sync:tokens`                               | пересобрать снимок таблицы токенов артборда (только на маке)           |
| `yarn sync:runes`                                | пересобрать снимок рунического алфавита (только на маке)               |
| `yarn test:unit`                                 | Vitest — токены направления и контраст пар «текст на фоне»             |
| `yarn test:e2e`                                  | Playwright — контраст на отрисованной странице в обеих темах и ширинах |
| `yarn build:lighthouse` + `yarn test:lighthouse` | Lighthouse CI, три прогона, порог по худшему из них                    |
| `yarn test`                                      | типы + линт + Prettier + юниты + e2e                                   |

## Витрина компонентов

`/components` — служебная страница со всеми компонентами направления в текущей теме. В пять страниц
демо не входит и на закрытом стенде будет закрыта.

## Темы

Тема лежит в куке `theme` (`dark` или `light`), сервер рендерит ее в `data-theme` на `<html>`.
Значение по умолчанию — темная. Переключателя в интерфейсе пока нет: он появится вместе с шапкой.

## Токены направления

`src/styles/tokens.scss` — перенос таблицы токенов артборда один в один: темная тема в `:root`,
светлая в `[data-theme='light']`, узкая ширина в `@media (max-width: 640px)`.

Артборд намеренно не входит ни в один git, поэтому на CI его нет. Тесты сверяют SCSS со снимком
таблицы `tests/fixtures/artboard-tokens.json`; на маке тот же тест пересобирает снимок из артборда
и падает, если он разошелся. После правки артборда — `yarn sync:tokens`.

## Проверки контраста

Их две, и обе обязательны:

1. `tests/unit/token-contrast.test.ts` — пары «текст на фоне» из таблицы токенов не ниже 4,5:1
   в обеих темах;
2. `tests/e2e/rendered-contrast.spec.ts` — каждый текстовый узел отрисованной страницы не ниже
   4,5:1 в обеих темах на 1440 и 390. Токенная проверка по устройству не видит текст поверх
   фотографий, градиентов и текстур — именно там сидели оба провала артборда.

Рядом `tests/e2e/shell.spec.ts` сторожит оболочку: язык страницы, тему по умолчанию и то,
что размеры переключаются ровно на брейкпоинте узкой колонки.
