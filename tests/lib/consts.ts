/**
 * Ширина, на которой включается узкая колонка артборда. Записана числом рядом с тестами, а не
 * вычитана из `tokens.scss`: смысл проверки в том, чтобы подмена числа в стилях роняла тесты.
 */
export const NARROW_BREAKPOINT = 640;

/**
 * Токены, которыми набирается текст. Четвертый токен артборда, `--font-runes`, остается ради паритета
 * с таблицей, но не используется: рунические надписи рисуются отрезками, и шрифт им не нужен.
 */
export const TEXT_FONT_TOKENS = ['--font-caps', '--font-display', '--font-text'] as const;
