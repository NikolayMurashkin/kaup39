/** Галерея: пять колонок на широкой ширине, две на узкой. */
export const GALLERY_SIZES = '(max-width: 640px) 50vw, 20vw';

/** Фотография рядом с текстом раздела: половина колонки, на узкой — вся ширина. */
export const SPLIT_PHOTO_SIZES = '(max-width: 640px) 100vw, 50vw';
