export type TokenSurface = {
  /** Токен фона поверхности. */
  background: string;
  /** Подпись поверхности на странице. */
  name: string;
  /** Токен, которым подпись покрашена: на киновари и железе основной текст порог не проходит. */
  nameToken: string;
  /** Токены текста, которые на этой поверхности разрешены таблицей артборда. */
  texts: { token: string; label: string }[];
};
